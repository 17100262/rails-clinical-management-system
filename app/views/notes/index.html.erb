<div class="container-fluid container-top" style="">

<div class="container">
<p id="notice"><%= notice %></p>
<div class="row m-b-xs">
    <div class="col-lg-12 text-right">
      <%= link_to 'New Note', new_user_note_path(current_user), class:"btn btn-primary m-t-xs" %>
        
    </div>
    
  </div>


<div class="table-responsive project-list">
<table class="table white-bg border-left-right border-top-bottom table-icons">
  <thead>
    
    <tr>
      <th> Title</th>
      <th> Description</th>
      <th class="actions">Actions</th>
      
    </tr>
  </thead>

  <tbody>
     <% @notes.each do |note| %>
      <tr>
        <td><%= note.title %></td>
        <td> <%= note.description.truncate(100) %></td>
        <td><%= link_to "<i class='far fa-folder-open icon-chat' title='Show'></i>".html_safe, note_path(note) %>
        <%= link_to "<i class='fas fa-edit icon-chat' title='edit'></i>".html_safe, edit_note_path(note) %>
        <%= link_to "<i class='far fa-trash-alt icon-danger icon-chat' title='Destroy'></i>".html_safe, note, method: :delete, data: { confirm: 'Are you sure?'} %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>

